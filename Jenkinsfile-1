pipeline {
    agent any

    tools {
        git 'Git'  // 👈 This forces Jenkins to use the correct Git tool
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                echo '✅ Build stage reached successfully!'
            }
        }
    }
}
