pipeline {
    agent any

    tools {
        git 'git' // ✅ match exactly with Global Tool Config name
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                echo '✅ Build stage reached successfully!'
            }
        }
    }
}
